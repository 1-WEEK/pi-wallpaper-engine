{
  "name": "@pwe/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "NAS-side transcoding worker — pulls TranscodeJob entries from the Pi backend and runs ffmpeg",
  "scripts": {
    "start": "bun src/index.ts",
    "dev": "bun --watch src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@pwe/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  },
  "engines": {
    "bun": ">=1.2.0"
  }
}
